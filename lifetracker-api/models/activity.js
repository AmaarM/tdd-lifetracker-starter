const { BadRequestError, UnauthorizedError, NotFoundError } = require("../utils/errors");
const db = require("../db");
const User = require("../models/user");


class Activity {

    static calculateDailyCaloriesSummaryStats(){
        
    }










}